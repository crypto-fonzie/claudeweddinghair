repo: crypto-fonzie/Noelle-Weddinghair
branch: main

## Last sync

date: 2026-08-09T01:25:15Z

### Updated in this project

- Associated the repository with this design system. No repository files imported yet.
- Read the repository tree only: `css/base.css`, `css/components.css`, `css/tokens.css`, and one planning document under `docs/superpowers/plans/`.
- The repository is currently near-empty (4 files, no HTML). The website in this project is the more advanced of the two and was NOT overwritten.
- Nothing in this project was changed by the association.

## Screen map

| Screen in this project | Built from |
| --- | --- |
| `templates/website/Homepage.dc.html` | authored here; no repo counterpart |
| `ui_kits/website/Home.jsx` | authored here; no repo counterpart |
| `ui_kits/website/Work.jsx` | authored here; no repo counterpart |
| `ui_kits/website/Morning.jsx` | authored here; no repo counterpart |
| `ui_kits/website/Enquire.jsx` | authored here; no repo counterpart |
| `ui_kits/website/Chrome.jsx` | authored here; no repo counterpart |

## Notes

The repository holds an earlier, unrelated start: three stylesheets and a long planning
document. It contains no HTML pages and no components. Before importing anything from
`css/`, check it against `tokens/` and `styles.css` here — this project has 121 resolved
tokens and a committed palette, and the repo's `tokens.css` may predate them.

Pushing this project INTO the repository has to be done from the downloaded zip; the
GitHub connection here is read-only.
